diff --git a/tools/CMakeLists.txt b/tools/CMakeLists.txt
index 780df3266..06e540ad5 100644
--- a/tools/CMakeLists.txt
+++ b/tools/CMakeLists.txt
@@ -12,32 +12,42 @@ llama_add_compile_flags()
 
 # tools
 
+option(LLAMA_BUILD_EXTRA_TOOLS "llama: build extra benchmark and cli tools" OFF)
+
 if (EMSCRIPTEN)
 else()
-    add_subdirectory(batched-bench)
-    add_subdirectory(gguf-split)
-    add_subdirectory(imatrix)
-    add_subdirectory(llama-bench)
-    add_subdirectory(completion)
-    add_subdirectory(perplexity)
-    add_subdirectory(quantize)
+    if (LLAMA_BUILD_EXTRA_TOOLS)
+        add_subdirectory(batched-bench)
+        add_subdirectory(gguf-split)
+        add_subdirectory(imatrix)
+        add_subdirectory(llama-bench)
+        add_subdirectory(completion)
+        add_subdirectory(perplexity)
+        add_subdirectory(quantize)
+    endif()
     if (LLAMA_BUILD_SERVER)
         add_subdirectory(ui)
-        add_subdirectory(cli)
+        if (LLAMA_BUILD_EXTRA_TOOLS)
+            add_subdirectory(cli)
+        endif()
         add_subdirectory(server)
     endif()
-    add_subdirectory(tokenize)
-    add_subdirectory(parser)
-    add_subdirectory(tts)
+    if (LLAMA_BUILD_EXTRA_TOOLS)
+        add_subdirectory(tokenize)
+        add_subdirectory(parser)
+        add_subdirectory(tts)
+    endif()
     add_subdirectory(mtmd)
     if (GGML_RPC)
         add_subdirectory(rpc)
     endif()
-    if (NOT GGML_BACKEND_DL AND GGML_CPU)
+    if (NOT GGML_BACKEND_DL AND GGML_CPU AND LLAMA_BUILD_EXTRA_TOOLS)
         # these tools use backends directly (no dynamic loading) and depend on CPU backend symbols
         add_subdirectory(cvector-generator)
         add_subdirectory(export-lora)
     endif()
-    add_subdirectory(fit-params)
-    add_subdirectory(results)
+    if (LLAMA_BUILD_EXTRA_TOOLS)
+        add_subdirectory(fit-params)
+        add_subdirectory(results)
+    endif()
 endif()
diff --git a/tools/mtmd/CMakeLists.txt b/tools/mtmd/CMakeLists.txt
index 15040e4af..436817c0e 100644
--- a/tools/mtmd/CMakeLists.txt
+++ b/tools/mtmd/CMakeLists.txt
@@ -129,7 +129,7 @@ endif()
 # builds (LLAMA_BUILD_MTMD=ON with LLAMA_BUILD_TOOLS=OFF — e.g. Apple
 # XCFramework packaging) skip the executables entirely. LLAMA_BUILD_COMMON
 # defaults to ON in standalone builds, so we cannot rely on it for gating.
-if (LLAMA_BUILD_TOOLS)
+if (LLAMA_BUILD_TOOLS AND LLAMA_BUILD_EXTRA_TOOLS)
     add_executable(llama-llava-cli    deprecation-warning.cpp)
     add_executable(llama-gemma3-cli   deprecation-warning.cpp)
     add_executable(llama-minicpmv-cli deprecation-warning.cpp)
